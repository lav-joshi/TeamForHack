import React, { Component } from 'react'
import AddProfile from './AddProfile'

export default class SignUp extends Component {
    render() {
        return (
            <React.Fragment>
               {/* 1.GOOGLE AUTH*/}
                {/* 2. Redirect to add profile(first time account) or Main Profile(else) */}
            </React.Fragment>
        )
    }
}
